from setuptools import setup

# All metadata, dependencies, and entry points are declared in pyproject.toml.
# This file exists solely so setuptools_scm can hook into the build process
# for automatic version detection from git tags.
setup()
